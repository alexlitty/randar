#include <randar/Render/VertexAttribute.hpp>

randar::VertexAttribute::VertexAttribute(unsigned int initLocation, unsigned int initSize, unsigned int initOffset)
: location(initLocation),
  size(initSize),
  offset(initOffset)
{

}
