#include <cmath>
#include <randar/math/Constant.hpp>

const float randar::PI                = std::atan(1) * 4;

const float randar::QUARTER_PI        = randar::PI / 4.0f;
const float randar::THREE_QUARTERS_PI = randar::QUARTER_PI * 3.0f;
const float randar::FIVE_QUARTERS_PI  = randar::QUARTER_PI * 5.0f;
const float randar::SEVEN_QUARTERS_PI = randar::QUARTER_PI * 7.0f;

const float randar::HALF_PI           = randar::PI / 2.0f;
const float randar::THREE_HALVES_PI   = randar::HALF_PI * 3.0f;

const float randar::TWO_PI            = randar::PI * 2.0f;
